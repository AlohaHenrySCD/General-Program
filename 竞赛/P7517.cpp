#include <iostream>
#include <cstdio>
#include <algorithm>
#include <vector>
#define ll long long
using namespace std;
const int N = 2e5+5;
int n, a[N], t[N];
ll ans;

int main(){
    cin >> n;
    for(int i=0;i<n;i++)
        cin >> a[i];
    sort(a, a+n);
    
}