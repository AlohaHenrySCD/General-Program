#include <cstdio>
#include <iostream>
#include <cstring>
#include <vector>
using namespace std;

int main(){
    // int dis[1000];
    // memset(dis, 0xff, sizeof(dis));
    // cout << dis[100];

    // cout << int(1e9);

    // int d[2][2] = {0, 1, 2, 3};
    // printf("%d %d %d %d\n", d[0][0], d[0][1], d[1][0], d[1][1]);
    // int t[2][2];
    // memcpy(t, d, sizeof(d));
    // printf("%d %d %d %d\n", d[0][0], d[0][1], d[1][0], d[1][1]);
    // printf("%d %d %d %d\n", t[0][0], t[0][1], t[1][0], t[1][1]);
    cout << 2100000000;
}